apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    app: prometheus
spec:
  selector:
    app: prometheus
  ports:
    - protocol: TCP
      port: {{ .Values.metrics.Prometheus.prometheus.port }}
      targetPort: {{ .Values.metrics.Prometheus.prometheus.port }}
  type: LoadBalancer # Use LoadBalancer if needed
