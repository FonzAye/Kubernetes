apiVersion: v1
kind: Service
metadata:
  name: mongo-express
  labels:
    app: mongo-express
spec:
  selector:
    app: mongo-express
  ports:
    - protocol: TCP
      port: {{ .Values.database.mongoExpress.port }}
      targetPort: {{ .Values.database.mongoExpress.port }}
  type: LoadBalancer